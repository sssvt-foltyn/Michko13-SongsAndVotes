@page "/song/create"
@inject IDialogService dialogService 

@code {
    DialogOptions maxWidth = new DialogOptions() { MaxWidth = MaxWidth.ExtraSmall, FullWidth = true, NoHeader = true };

    protected override void OnInitialized()
    {
        dialogService.Show<CreateSongDialog>("", maxWidth);
    }
}
